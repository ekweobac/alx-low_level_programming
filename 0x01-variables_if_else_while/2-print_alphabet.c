#include <stdio.h>

/**
 * main - print alphabets in lowercase with putchar
 *
 * Description: you can only use the putcha function
 * everyother function is forbidden
 * Return: 0
 */

int main(void)
{
char alphabet;
for (alphabet = 'a'; ch <= 'z' alphabet++)
{
	putchar(alphabet);
}
putchar('\n');
return (0);
}
